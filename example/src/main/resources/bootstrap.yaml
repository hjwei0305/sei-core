spring:
  application:
    name: sei-auth
  cloud:
    config:
      profile: DEV
      name: ${spring.application.name}
    zookeeper:
      enabled: false

sei:
  # 本地调试的当前用户配置
  mock:
    user:
      tenant-code: 10044
      account: admin
      user-name: 管理员

  log:
    remote:
      host: 10.4.208.132:5044